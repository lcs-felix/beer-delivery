spring:
  profiles:
    active: dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
        jdbc:
          lob:
            non_contextual_creation: true

---
spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/beer_dev
    username: postgres

---
spring:
  profiles: test
  datasource:
    url: jdbc:postgresql://localhost:5432/beer_test
    username: postgres